# 表示したい COG の URL。R2 の公開 URL を想定。
# 例: https://<bucket>.<account>.r2.dev/noto-csmap-2024-cog.tif
VITE_COG_URL=

# 開発時にローカルの巨大 COG を HTTP Range で配信するディレクトリ。
# 指定すると /local/<相対パス> で読めるようになる（dev サーバー限定）。
LOCAL_RASTER_DIR=
